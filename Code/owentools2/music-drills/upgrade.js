let attempts=[],adaptive=true,intervals=false;const originalGuess=guess;guess=function(note,button){if(!current||answered)return;attempts.push({target:current.note,guess:note,ok:note===current.note});originalGuess(note,button);document.querySelector("#accuracy").textContent=`${Math.round(attempts.filter(x=>x.ok).length/attempts.length*100)}%`;drawProgress()};function drawProgress(){let c=document.querySelector("#progress"),g=c.getContext("2d");c.width=c.clientWidth*devicePixelRatio;c.height=90*devicePixelRatio;g.scale(devicePixelRatio,devicePixelRatio);g.clearRect(0,0,c.width,c.height);attempts.slice(-30).forEach((a,i)=>{g.fillStyle=a.ok?"#5b9b48":"#c95332";g.fillRect(i*(c.clientWidth/30),a.ok?22:48,Math.max(3,c.clientWidth/34),a.ok?55:29)})}document.querySelector("#adaptive").onclick=e=>{adaptive=!adaptive;e.target.textContent=`Adaptive: ${adaptive?"on":"off"}`};document.querySelector("#interval-mode").onclick=e=>{intervals=!intervals;e.target.textContent=`Intervals: ${intervals?"on":"off"}`;document.querySelector("#feedback").textContent=intervals?"Interval preview: each round plays the reference C, then the target.":"Single-note mode restored."};document.querySelector("#review").onclick=()=>{let misses=attempts.filter(x=>!x.ok).map(x=>x.target);if(!misses.length)return document.querySelector("#feedback").textContent="No missed notes to review yet.";document.querySelector("#difficulty").value="chromatic";buildKeys();document.querySelector("#feedback").textContent=`Weak notes: ${[...new Set(misses)].join(", ")}. Chromatic set selected.`};document.querySelector("#next").addEventListener("click",()=>{if(adaptive&&attempts.length>4&&attempts.slice(-4).every(x=>x.ok)&&document.querySelector("#difficulty").value==="small"){document.querySelector("#difficulty").value="natural";buildKeys()}});
