const canvas=document.querySelector('#game'),ctx=canvas.getContext('2d'),msg=document.querySelector('#message'),W=canvas.width,H=canvas.height;let score=[0,0],playing=false,last=performance.now();const paddle={x:0,y:.82},bot={x:0,y:.12},ball={x:0,y:.68,z:.08,vx:0,vy:0,vz:0};
function project(x,y,z=0){const s=1-y*.64;return{x:W/2+x*W*.42*s,y:H*.12+y*H*.77-z*H*.35,s}}function reset(){Object.assign(ball,{x:0,y:.68,z:.09,vx:0,vy:0,vz:0});playing=false;msg.textContent='Click to serve';msg.hidden=false}function serve(){if(playing)return;playing=true;msg.hidden=true;Object.assign(ball,{vy:-.62,vx:(Math.random()-.5)*.22,vz:.55})}function hit(player){const p=player?paddle:bot;ball.vy=(player?-1:1)*(.58+Math.random()*.12);ball.vx=(ball.x-p.x)*.7+(player?(paddle.x-ball.x)*-.22:(Math.random()-.5)*.18);ball.vz=.62}function point(who){if(!playing)return;playing=false;score[who]++;document.querySelector(who?'#you':'#bot').textContent=score[who];msg.textContent=who?'Point to you':'Robot scores';msg.hidden=false;setTimeout(reset,700)}
function update(dt){if(!playing)return;bot.x+=(ball.x-bot.x)*Math.min(1,dt*2.5);ball.x+=ball.vx*dt;ball.y+=ball.vy*dt;ball.z+=ball.vz*dt;ball.vz-=1.6*dt;if(ball.z<0){ball.z=0;ball.vz=Math.abs(ball.vz)*.82}if(ball.y<.15&&ball.vy<0&&Math.abs(ball.x-bot.x)<.27)hit(0);if(ball.y>.79&&ball.vy>0){Math.abs(ball.x-paddle.x)<.25?hit(1):point(0)}if(ball.y<-.08)point(1);if(Math.abs(ball.x)>1.05)point(ball.vy<0?1:0)}function poly(ps,fill,stroke){ctx.beginPath();ps.forEach((p,i)=>i?ctx.lineTo(p.x,p.y):ctx.moveTo(p.x,p.y));ctx.closePath();ctx.fillStyle=fill;ctx.fill();ctx.strokeStyle=stroke;ctx.lineWidth=3;ctx.stroke()}
function drawPaddle(p,color){const q=project(p.x,p.y,.03);ctx.fillStyle=color;ctx.beginPath();ctx.ellipse(q.x,q.y,80*q.s,27*q.s,0,0,7);ctx.fill()}function draw(){ctx.fillStyle='#120b09';ctx.fillRect(0,0,W,H);poly([project(-1,0),project(1,0),project(1,1),project(-1,1)],'#b74a20','#ff9a5c');let a=project(-1,.5,.02),b=project(1,.5,.02);ctx.strokeStyle='#fff4dd';ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke();drawPaddle(bot,'#ead7bb');drawPaddle(paddle,'#ff7025');const q=project(ball.x,ball.y,ball.z);ctx.fillStyle='#fff';ctx.beginPath();ctx.arc(q.x,q.y,Math.max(6,13*q.s),0,7);ctx.fill()}function loop(t){update(Math.min(.025,(t-last)/1000));last=t;draw();requestAnimationFrame(loop)}
canvas.onpointermove=e=>{const r=canvas.getBoundingClientRect();paddle.x=Math.max(-.85,Math.min(.85,((e.clientX-r.left)/r.width-.5)*2))};canvas.onpointerdown=()=>{if(!playing)serve();else if(ball.y>.62&&ball.vy>0)hit(1)};function restart(){score=[0,0];document.querySelector('#you').textContent=0;document.querySelector('#bot').textContent=0;reset()}document.querySelector('#restart').onclick=restart;addEventListener('keydown',e=>{if(e.key.toLowerCase()==='r')restart()});reset();requestAnimationFrame(loop);
